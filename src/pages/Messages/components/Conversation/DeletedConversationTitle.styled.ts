import { styled } from "styled-components";

const DeletedConversationTitle = styled.div`
  font-family: Inter;
  font-style: normal;
  font-weight: 600;
  font-size: 14px;
  color: white;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
`;

export default DeletedConversationTitle;
